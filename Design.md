# 面向图基础模型的多模态属性图数据评估基准

## 研究背景	

​	近年来，基于大型语言模型（LLM）的研究不断涌现，各大技术报告与学术文献反复强调：**预训练阶段的“数据管理与数据质量”直接制约模型性能的上限**。换言之，无论模型规模如何扩张，如果预训练语料存在噪声过多、异构性强或分布不均等问题，其在下游任务中的表现都难以显著提升。随着知识图谱（KG）与图神经网络（GNN）技术的成熟，研究者逐渐探索将图结构信息与大模型预训练相结合，催生了所谓的图基础模型（Graph Foundation Model, GFM）。这类模型需要海量的**多领域、多模态属性图（Multimodal Attribute Graph, MAG）**作为预训练语料。然而，当前能够用于GFM预训练的MAG数据集存在异构度与噪声多、领域分布不均、缺乏统一的语料量与选择标准、下游任务不明确等问题。

​	图基础模型（Graph Foundation Model，GFM）是近年来新兴的研究方向，旨在训练一个统一的图模型以适应多种下游图任务 。与自然语言和视觉领域的大模型类似，GFM 的训练依赖大规模、高多样性的图数据作为基础。但是，目前针对图数据的大模型研究仍处于起步阶段，**一个关键瓶颈在于缺乏统一规范的多模态图数据支撑**。**多模态属性图**（Multimodal Attributed Graph, MAG）的提出正是为了解决这一问题：MAG 可以看作传统属性图的泛化版本，其中节点不仅包含单一模态的属性（如文本），还融合了多种模态的信息（如图像、文本等）。传统的**文本属性图**（Text-Attributed Graph, TAG）是属性图的特例，每个节点仅附带文本描述等单模态属性；相比之下，MAG 在理论上扩展了 TAG 的内涵，允许节点承载多种模态属性，从而能表达更丰富的知识。

​	现有的图表示学习工作大多侧重于拓扑结构本身，对节点属性的利用相对有限。早期研究逐渐开始关注属性图表示学习，但主要局限于单模态属性场景，例如将 GNN 应用于文本属性图以融合节点的文本特征与图结构。这类研究忽视了多模态属性与拓扑结构之间复杂的关联，可能导致次优的表示效果。与此同时，知识图谱领域也出现了引入多模态信息的趋势，即**多模态知识图谱**（Multimodal Knowledge Graph, MMKG），其在传统知识图谱的基础上为实体附加图像、文本等信息。但总体而言，无论是 MAG 表示学习还是 MMKG 嵌入，目前都缺乏标准化的数据集和评估框架，限制了该领域的进一步发展 。在数据管理角度来看，如何构建面向 GFM 训练的数据层支持、制定统一的结构规范，成为当前迫切需要解决的问题。本工作正是针对这一需求，提出了一个多模态属性图数据评估基准，从数据管理与组织的层面为图基础模型的训练提供支持。通过规范数据格式、涵盖多领域多模态内容并量化数据质量，我们为 GFM 的预训练提供了高质量的数据基础和结构性规范，有望加速该领域研究的进展。

### 1.Difference between MAG and TAG

| 维度         | MAG                                                          | TAG                                    |
| ------------ | ------------------------------------------------------------ | -------------------------------------- |
| 数据模态     | 文字 + 图像 (+ 音频/视频/文档)                               | 仅文字属性                             |
| 节点/边属性  | 多模态特征向量、多模态caption generation                     | 文本embedding                          |
| 跨模态一致性 | 需评估文本-图像/音频/文档语义对齐 (CLIPScore, VLM similarity) | 不涉及跨模态一致性                     |
| 模型需求     | 图编码器 + 跨模态对齐模块 (CLIP/ViT + Text Encoder)          | 传统 GNN / 纯文本 Transformer 即可     |
| 优势         | 融合视觉与语言，信息量更丰富; 适用于视觉-文本混合场景        | 简洁高效，易于训练; 对文本推理任务友好 |

### 2.Difference between MAG and MMKG

​	虽然 MAG 和 MMKG 都涉及多模态信息与图结构，但二者关注的模型架构和任务存在显著差异。表2总结了 MAG（多模态属性图）与 MMKG（多模态知识图谱）在若干关键维度上的区别。

<table>
<tr><th>对比维度</th><th>多模态属性图（MAG）</th><th>多模态知识图谱（MMKG）</th></tr>
<tr><td>图结构与语义</td><td>一般为属性图，可为同构或异构网络，边关系通常不限定固定语义类型（如社交关系网、商品用户交互网等）；节点承载文本、图像等多模态属性描述。</td><td>基于知识图谱结构，包含实体节点及有语义类型的关系边（三元组形式）；实体可附加多模态属性作为补充信息，但核心结构是由明确语义关系连接的知识网络。</td></tr>
<tr><td>典型模型架构</td><td>采用图表示学习方法（如图神经网络,GNN），将节点多模态属性作为特征输入，通过消息传递机制结合拓扑结构学习节点表示。</td><td>采用知识图谱嵌入方法（KGE，如 TransE 系列），将多模态信息通过预训练编码（文本编码器、视觉模型等）转化为实体的补充向量，再融入KG嵌入模型以学习实体和关系的表示。</td></tr>
<tr><td>下游任务及评估</td><td>面向通用图任务，如节点分类、链路预测、社区发现、图分类等。评估侧重于模型在同一图或跨图场景下的性能（如节点标签预测准确率、新链路识别率），强调对未知图/节点的泛化能力。</td><td>面向知识库Completion等特定任务，如知识图谱补全（链接预测）、实体对齐/分类、问答等。评估通常基于三元组预测的准确性（如Hit@K、MRR），关注模型对已知KG中缺失关系的推理和对实体多模态属性的利用。</td></tr>
<tr><td>训练输入/输出形式</td><td>通常以整个图或其子图作为输入。模型读取节点的多模态属性特征和邻接关系，输出各节点的低维向量表示或任务相关预测（如节点所属类别）。训练可采用有监督或自监督方式（如基于节点标签或对比学习）。</td><td>通常以三元组形式训练：输入一个(head, relation, tail)三元组及其多模态特征（如head实体的图像、文本），模型输出一个评分或嵌入向量，以判断三元组的合理性。训练多为无监督/自监督（利用正负三元组样本进行排名损失优化）。</td></tr>
<tr><td>多模态信息的集成</td><td>节点的多模态属性直接作为模型特征参与图神经网络计算，多模态融合发生在消息传递和节点表征更新过程中。</td><td>实体的多模态信息通常通过独立的模态编码器获取（如图像CNN特征、文本预训练模型语义向量），再与结构信息结合。在KG嵌入训练时，多模态特征要么作为额外约束融入损失函数，要么用于后处理增强，但不直接改变KG结构本身。</td></tr>
</table>



​	MAG 更侧重于在**通用图结构**上融合多模态节点特征来提升图表示学习的效果，而 MMKG 则专注于**知识图谱**情境下利用多模态信息改进知识推理和表示。二者在方法上有区别，但在本质上都体现了多源信息融合以提升模型认知能力的思想。本工作聚焦于 MAG 数据集的构建与评估，因为我们立足于服务图基础模型训练的通用数据需求；同时，我们的研究方法论也能为 MMKG 等领域的数据构建提供有益借鉴。

## 研究动机

​	图基础模型的成功很大程度上依赖于训练数据的**广度**、**规模**和**质量**。与自然语言和计算机视觉的预训练类似，我们需要确保图数据在覆盖领域、数据量和数据可靠性方面足够丰富和健全，才能学得通用而强大的表示。然而，目前可用的图数据集往往在这几个方面存在不足。例如，许多数据集只涵盖单一领域、规模有限，或多模态信息不完备。这些不足限制了预训练模型的泛化性能。基于此，我们从以下三个维度细化研究动机并定义核心问题：

### **1.数据广度（跨领域多样性）**

​	**数据广度**指训练数据所覆盖的领域和语义多样性。对于图基础模型而言，训练数据如果局限于单一领域（例如仅社交网络或仅引文网络），模型可能只能捕捉特定领域的结构模式和语义，从而难以泛化到其它未知领域的图数据。相反，跨领域的多样化数据能够提供更广泛的结构模式和节点语义，使模型学习到更加普适的表示。本研究以**领域多样性**为切入点，探究训练数据涵盖的领域数量对模型泛化能力的影响。我们的假设是：随着训练图涵盖领域数的增加，模型对新领域、新任务的适应能力将提升。然而，领域数量并非越多越好，当领域过于分散、语义差异极大时，模型可能难以同时学到所有领域的有用特征，甚至不同领域间的冲突会干扰模型学习。因此，我们关注是否存在一个**领域数阈值**——即领域多样性达到一定程度后，模型性能的提升趋于饱和或出现反降。此外，我们关注**语义覆盖面**与模型泛化性能的关系：训练数据语义空间覆盖越广，是否一定带来下游更高的性能，抑或需要平衡领域广度与每个领域内数据深度。这些问题直接关系到多领域数据对于图基础模型训练的价值，我们将在评估中系统分析数据广度这一维度。

### **2.数据数量（样本规模）**

​	**数据数量**指训练数据的规模，包括节点和边的数量、以及节点所附属性的总量等。大规模数据是预训练模型成功的基石之一。在图领域，同样存在“大数据效应”：更多的节点和关系可以使模型学习到更丰富的结构模式和特征分布，从而提升表示的泛用性。然而，受制于获取成本和现有数据集限制，许多研究使用的图数据集规模相对有限，难以验证**规模扩展**对模型性能的影响。本研究将数据规模作为独立维度，设计对比实验来量化其作用。一方面，我们构建**小规模语料**与**大规模语料**两组训练数据，通过控制其他因素（领域、质量）保持一致，仅改变样本数量，来观察模型性能差异。另一方面，我们尝试探究规模报酬递减现象：当数据增加到一定量时，模型性能增益是否趋于平缓？亦或在我们实验范围内，性能随数据量近乎单调提升？这些分析将揭示图基础模型是否存在类似语言模型的*Scaling Law*，为后续数据收集提供指引。如果实验证明大规模数据确实带来显著性能提升，那么构建更大规模的MAG数据集将是值得投入的方向；反之，如果出现饱和，则说明仅靠增加数据量可能不足以进一步提升模型，需要从质量或模型架构上寻找突破。

### **3.数据质量（模态完备性与语义一致性）**

​	**数据质量**在多模态属性图情境下主要体现在两个方面：其一是**模态完备性**，即节点的多模态属性是否齐全；其二是**语义一致性**，即节点的不同模态信息在语义上是否匹配。一份高质量的MAG数据应确保大部分节点同时具有文本、图像等属性，而且这些属性应当相互佐证、描述同一实体的语义。然而，现实数据常存在模态缺失和不一致的问题：例如某些产品节点缺少商品图片，或者社交媒体帖子附带的图片与文本内容不相关。这种质量问题可能对模型训练产生负面影响——模态缺失会使模型无法利用对应模态的信息，语义不一致则可能混淆模型的表示学习。本研究引入一个综合的**质量评分 Q**来量化数据的模态完备性和一致性。我们根据每个节点的模态情况进行打分：同时具有完整文本和图像的节点记为高完备性，否则降低评分；再利用预训练模型提取图像和文本的语义向量，计算二者的相似度来评估一致性，作为另一部分评分。将两方面结合得到节点或数据集的质量得分 Q。在问题定义上，我们关注**数据质量与模型性能的关联**：高质量的数据是否显著提升模型表现？能否通过质量评分预测或解释模型性能变化？具体而言，我们将数据按 Q 值划分不同档次（如高、中、低质量组），分别用于模型训练或评价，观察模型效果的差异。同时，我们检验模型性能指标与 Q 值的相关性，以验证质量指标的有效性。通过这一系列分析，我们期望明确提升数据质量（补全模态信息、过滤语义不一致样本）对模型训练的正面作用，并据此制定数据清洗和质量控制的准则。

###  **三维度交互影响与研究问题**

​	上述**数据广度**、**数据数量**和**数据质量**三者并非相互独立，而是在实际数据集构建中交织影响模型性能。一份数据集可能同时在某些维度上占优、在另一些维度上欠缺。例如，包含跨多个领域的大规模数据往往来源多样，可能伴随质量良莠不齐；反之，一个限定领域的小型数据集或许质量精良、一致性高，但缺乏多样性和规模支持。因此，有必要研究这些维度的交叉组合对 GFM 训练效果的影响，以及如何在设计基准时平衡取舍。本工作在实验设计中纳入了一系列**交叉对照**：比如，比较“广+多但质低”（涵盖领域广、数据量大但质量偏低）的数据与“狭+少但质高”（领域单一、规模较小但质量精挑细选）的数据，对比二者对模型性能的影响，以判断哪个因素更为关键。又例如，我们构造“广+多+质高”的理想场景作为上限基准，以及“狭+少+质低”的劣质场景作为下限基准，用于衡量实际数据集相对于理想情况的差距。这些交叉实验将帮助我们识别：当资源有限时，应该优先保证质量还是扩增数量/广度？ 是否存在某一维度具有更显著的影响力？ 针对这些问题的回答，不仅有助于解释实验现象，更将沉淀为**基准数据集设计的方法论**。换言之，我们希望提炼出一套原则，指导今后构建多模态属性图基准时，在领域覆盖、数据规模、数据质量三方面达到最优平衡，以最大化图基础模型的训练效果。

## RQ&预期结果

| #      | Research Question                                            | Expected Findings                                            |
| ------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **Q1** | **数据广度临界点**：跨领域多样性究竟增加到什么范围后，GFM 的泛化收益会趋于饱和甚至反降？ | 存在可观测“**领域拐点**”：• 前 2 – 3 个主干领域带来显著增益；• 第 4〜5 个领域后边际提升 ≤ 3%，并对原有领域出现轻微干扰；• 建议在保证语义覆盖的前提下，**优先精选 4 ± 1 个代表性领域**。 |
| **Q2** | **数据规模效益曲线**：样本数量无限放大是否持续抬高性能？何时出现回报递减？ | 呈现类 *Scaling-law* 曲线：• 0 → 0.5 全集：性能大幅跃升（≈ +10 % F1）；• 0.5 → 0.75：仍有明显收益（≈ +5 %）；• 0.75 → 1.0：收益减缓（≈ +2 ~ 3 %），但尚未完全饱和；• **进一步增量可带来提升，但性价比逐步降低**——需结合算力／标注成本评估。（也可能0.9→1.0的时候性能下降） |
| **Q3** | **质量 vs. 数量**：高质量小语料能否击败低质量大语料？Q 值与性能是否线性相关？ | 结论基本肯定：• 小-高质模型平均比大-低质模型高；• Q 与多任务性能 Pearson ρ > 0.9，呈近线性正相关；• **先确保 Q ≥ 阈值（完备性+一致性）再扩充数量**是最稳妥路径。 |
| **Q4** | **三维度权重优先级**：在资源受限场景，质量、广度、数量应如何排序？ | 实证排序：**质量 > 广度 > 数量**。• 低质噪声会显著拉低上限；• 在同等质量下，增加领域多样性比等量扩充同域样本更有效；• 当两者都达到合理水平后，再扩大规模可获额外增益。 |
| **Q5** | **“先粗后精”策略可行性**：先用大-低质数据预训练，再用小-高质数据微调，能否兼得两者优势？ | 结果显示 **Group C（先大后小微调）≈ 最佳**：• 比纯高质训练再高 ≈ +5 % F1；• 明显优于“先精后杂”顺序（Group D）；• 佐证**“广覆盖初始化 + 精细对齐”**的有效范式，可作为推荐训练流程。 |

## 实验设计

​	以[Multi-Modal Knowledge Graph Construction and Application: A Survey](https://arxiv.org/abs/2202.05786)、[Mosaic of Modalities: A Comprehensive Benchmark for Multimodal Graph Learning](https://arxiv.org/abs/2406.16321)和When Graph meets Multimodal: Benchmarking and Meditating on Multimodal Attributed Graphs Learning为基础。

### 下游任务分析

- **图分类任务：**输入是一个完整的图或若干图实例（每个图含有多模态节点及边结构），输出是对整个图的类别判定。例如，在**生物分子图中预测分子功能类别，或在社交网络子图中预测社区类型**。由于MAG基准中大多是单一大图的节点分类，不直接涉及图分类，我们可以将其扩展为：从大图中抽取子图（如用户交互子网络）作为独立样本进行分类。对每个图做若干层消息传递以汇聚全图的信息，并在最后加一层读出层（例如对节点特征做池化或用图注意力读出一个全图表示）。在多模态情况下，可以使用前述融合机制在消息传递过程中融合节点的图文属性，使得图的表示综合拓扑和内容。采用**准确率、F1**等指标评估整图分类的准确性。
- **节点分类任务**：输入是一张大的多模态图和其中一部分节点的标签，模型需要预测未标注节点的类别。**预处理**过程中，要将节点的文本和图像属性转化为数值特征：可利用PLM和PVM（预训练视觉模型）分别将文本和图片编码成嵌入向量。并且根据需要对文本做截断、对图像做尺寸归一化等处理。**模型结构**方面，选择**GNN-As-Predictor**范式，即使用图神经网络来整合多模态特征和拓扑结构。
- **知识图谱推理 / 链接预测**：输入是一张多模态属性图（MAG）或多模态知识图谱（MMKG）中的一系列实体节点与关系信息，任务目标是预测图中缺失的链接或推理潜在的实体关系（一般预测尾实体）。在多模态情境下，实体节点除了结构特征外，还携带文本描述、图像甚至音频或视频等丰富属性，模型需要综合这些跨模态信息进行精准推理。技术上可采用MGFM编码器提取实体节点的多模态嵌入，再结合经典知识图谱嵌入（KGE）方法（如TransE、DistMult、RotatE）或跨模态融合评分函数计算实体关系的相似度或可信度。此外，也可结合路径推理方式（Path-based Reasoning）（我的MoKGR和科广组的RED-GNN&AdaProp），通过多跳图路径聚合更多上下文信息进行预测。
- **多模态生成与解释任务：这类任务要求模型根据图及其中的多模态信息生成新的模态内容**，例如生成对节点或子图的解释性文本描述，或者根据图生成图像/声音等。其中典型的有两种：**图→文本生成**和**图→图像生成**。前者如让模型解释两商品为何经常被一起购买、为某个节点生成描述摘要，后者如根据社交网络某个用户及其兴趣图谱生成可能喜欢的图片等。实现这类任务往往需要将**图基础模型（MGFM）的表示能力与大型生成模型（LLM或扩散模型）的语言/图像生成能力相结合**。
  - **图→文本解释**：输入通常是图中的一个节点或一个子图，我们希望生成一段自然语言来解释该节点的属性、其邻居关系，或总结子图的语义。例如，对于电商网络中的某商品节点，解释“该商品与XX类别的产品常被一起购买，因为它们功能互补”；又如在知识图谱中，为某一个实体子图生成百科式描述。技术上，这可以通过**MGFM + LLM**链式结合实现：首先利用训练好的图基础模型提取节点及其邻域的嵌入表示或检索相关信息，然后将这些信息格式化为prompt供LLM生成文本。
  - **图→图像生成**：这是更具挑战的新兴任务，即根据图结构和节点属性来生成新的图像。[InstructG2I](https://instructg2i.github.io/#:~:text=We%20propose%20a%20new%20task,InstructG2I%20to%20tackle%20this%20problem)提出了Graph2Image任务，要求模型以多模态图作为条件生成图像。例如，给定一个目标节点及其邻居（包含一些图像和文本标签），生成一个符合该节点语义的图像。该任务的难点在于图可能很复杂（节点多）且存在依赖关系，同时需要图生成的图像可控且反映图中信息。对于MGFM，可以直接将MGFM与MLLM进行配合，将节点经过MGFM消息传递后直接让MLLM生成新图像。

#### 评价指标

1. Accuracy，F1
2. MRR，H@1, H@10
3. 针对3和4任务的人工评分或相似度评分

​	为验证上述维度因素对图基础模型训练的影响，我们设计了系统的评估方案，将实证分析划分为三大板块，对应数据广度、数据数量和数据质量三个维度，并辅以跨维度的组合实验。所有实验均基于我们构建的多模态属性图基准数据集进行。在设计过程中，我们尽量控制非目标维度不变，以便客观衡量单一因素的作用。下面分别介绍各维度的评估设计和指标设定。

### 1.**数据广度维度分析**

​	在数据广度方面，我们通过**逐步增加领域覆盖**的方式，观察模型性能随领域多样性的变化趋势。具体做法是：从基准数据集中挑选若干个领域（例如电商、社交、学术等）的子图，根据需要构建不同“领域数”组合的训练集。起始情况下，我们选取其中一个领域的数据训练模型，并评估其在该领域内和跨领域任务上的表现。随后，依次引入第二个、第三个领域的数据加入训练集，每次扩展训练数据的领域多样性，同时评估模型在各领域测试集上的性能。通过这样的增量设计，我们可以获得**领域数量 vs. 模型性能**的关系曲线。评估指标方面，对于节点分类等任务，我们记录模型在各领域测试集上的准确率或F1值；对于链路预测等任务，则记录命中率Hit@K、MRR等指标的变化。

其中，我们重点关注以下**两个关键现象**：

1. **领域覆盖阈值效应**：是否存在领域数量达到某个值后，模型在新领域上的性能增益变得趋缓甚至下降？
2. **遗忘或干扰效应**：当加入新的领域训练时，模型在原有领域上的性能是否保持，抑或出现因干扰造成的精度下降。

​	为此，我们在评估时不仅比较跨模型的性能，还特别监测每次增量训练对各领域任务的影响差异。如果广度确实提升了模型的普适性，我们预期看到随领域增加，模型在总体上的平均性能提升；反之，如果过多领域导致干扰，可能会出现某点后曲线趋平甚至略降。此分析有助于确定训练数据应涵盖的**最佳领域多样性范围**，为构建广覆盖的预训练语料提供依据。

### 2.**数据数量维度分析**

​	在数据数量维度，我们设计**小规模 vs. 大规模**的对照实验来评估数据量对模型性能的影响。具体地，我们从完整基准数据集中抽取一个子集，控制其领域组成与完整数据相同，但缩减节点和边的数量，形成一个“小语料”数据集。例如，如果完整数据包含每个领域10万个节点，小语料可能各取1万节点，总规模减少一个数量级。在该小语料上训练图基础模型，并与在完整“大语料”上训练的模型进行对比。我们确保两者使用相同的模型架构和超参数，训练迭代次数也相当（或根据数据量调整迭代使总训练“见”到的样本数相同），以公平比较效果差异。评估指标包括各项下游任务的准确率、F1、Hit@K等，以及整体的表示迁移性能。

**预期结果**呈现**大数据优势**：大规模数据训练的模型在绝大多数任务上显著优于小规模数据的模型。

​	我们可能进一步探讨**扩展曲线**：例如分别取全数据的50%、75%、100%进行训练，观察性能提升的幅度。如果性能随数据量增加而持续提升且未显著饱和，则说明在当前模型容量下仍有“数据饥渴”，更多数据可能继续带来收益；若发现75%和100%数据训练的性能几乎相当，则暗示存在**饱和点**。这些发现将帮助我们判断目前数据规模是否足够，以及未来是否需要收集更大规模的数据来训练更强的GFM。

### 3.**数据质量维度分析**

​	在数据质量维度，我们首先定义并计算**质量评分 Q**。为全面量化模态完备性与语义一致性，我们从节点层面和整体数据层面分别评估：

* **数据质量维度**：把“好数据”拆成五条主线：完备性（节点是否同时有文本与图像）、干净度（文本是否少拼写错误、图片是否清晰）、一致性（文本-图像语义是否对得上）、结构性（图本身是否连通、是否存在孤立噪声节点）、有效性（在真实模型上训练后评价指标的表现）。**针对每条主线选 2-3 个可脚本化指标**。例如干净度用 GPT-2 Perplexity + 非语言字符占比；一致性用 CLIPScore；结构性用最大连通分量比例、有效性用F1，具体案例如下。
* **模态完备性指标**：计算数据集中节点具有完整多模态属性的比例。例如，统计有多少百分比的节点同时拥有文本和图像属性。记该比例为 $P_{\text{complete}}$。如果有额外模态（音频等）也可纳入类似计算。
* **语义一致性指标**：对每个具有文本-图像的节点，利用预训练的多模态模型或分别训练的文本、图像编码模型，提取文本描述的向量表示 $v_t$ 和图像的表示 $v_i$，计算它们的语义相似度（例如余弦相似度）$s = \cos(v_t, v_i)$。将所有节点的相似度取平均，得到数据集的平均跨模态一致性得分 $\bar{s}$。同时也可以计算分布，比如多少比例的节点相似度高于某阈值。
* **综合质量评分 Q**：我们将上述模态完备性比例和语义一致性分数进行加权或结合形成一个综合评分。例如，可定义 $Q = \alpha \cdot P_{\text{complete}} + \beta \cdot \bar{s}$，其中 $\alpha,\beta$权衡两方面的重要性（或通过其它非线性方式结合）。为简化分析，也可分别考察完备性和一致性对性能的影响，但综合的单一指标便于对数据集进行整体质量排序。

​	有了质量评分后，我们将数据集按照 Q 值划分档次（如高质量组和低质量组），用于对照实验。具体来说，我们从完整数据中挑选**高质量子集**（如选取 Q 分数最高的一定比例节点及其相应的边构成子图），以及**低质量子集**（选取 Q 最低的一定比例的数据）。这两个子集在规模上尽量相当，领域组成也保持一致，**唯一区别是数据质量高低**。然后分别用高质量数据和低质量数据训练模型，在相同测试集上评估其性能差异。如果我们的方法论假设成立，预期高质量数据训练的模型将明显优于低质量数据模型。这将直接证明数据质量对模型训练效果的影响。同时，我们在完整数据上训练的模型性能应介于高质和低质模型之间；若发现完整模型接近高质模型，则说明低质数据对整体影响不大，反之若完整模型性能被低质部分拖累明显，则提示需要考虑对训练数据进行质量筛选。在指标设定上，我们会比较高/低质模型在各项任务上的准确率提升幅度，并计算**质量评分与性能指标的相关系数**（例如 Pearson 或 Spearman 相关系数），验证**Q 值与模型性能的对齐**程度。如果相关性显著且高质模型优胜明显，就可以支持这样一个结论：提高数据的模态完备性和语义一致性（即提高 Q）能够带来图基础模型性能的提升，反之忽视数据质量可能导致模型表现受限。

### 4. **交叉策略对照与基准设计方法论**

​	除了分别考察单一维度外，我们还设计了一系列**交叉策略**实验，将广度、数量、质量维度组合起来进行对照，以更深入了解它们的相对重要性和相互作用。典型的组合策略包括：

1. **广覆盖大规模但低质量的数据 vs. 窄领域小规模但高质量的数据**：这两种极端策略代表了现实中可能遇到的抉择——是应当收集海量但噪声较大的跨域数据，还是精选小规模高质量的数据进行训练？通过对比这两种模型在相同测试集上的表现，我们可以判断哪个因素对模型最终性能的贡献更大。如果前者性能更佳，说明数据的覆盖面和数量优势足以抵消一定质量问题；如果后者更好，则强调了质量的重要性，暗示宁可舍去部分数据也要保证可靠性。+
2. **广覆盖大规模高质量 vs. 窄领域小规模低质量**：作为对照，我们将几乎“理想”的数据（既广又大且高质）与“最差”数据（窄、少且低质）进行比较。这一方面为模型性能提供上下限的参考，另一方面也能量化理想数据相比糟糕数据的提升倍数，凸显构建优质数据集的价值。
3. **中等权衡策略**：在实际应用中，可能存在介于上述两极之间的折中方案。例如，**广而精**（领域广且高质量但可能中等规模）与**狭而多**（领域单一但大规模且质量中等）之间如何选择，也是值得考察的问题。我们可通过设置不同组合的训练集，观察模型性能随组合变化的趋势，从中寻找较优的平衡点。

​	通过这些组合对照实验，我们希望归纳出一套**基准设计方法论**：即在构建多模态属性图数据集时，各维度的优先考虑顺序和权重如何。同时结合**最优预训练划分设计**，进行进一步实验补充。

###  最优预训练划分设计：先找拐点，再配比例

1. 先过滤低质量领域：统计样本量与平均 Q，把极小（<1 % 总量）且低质（平均 Q < 门限）的领域暂时排除。
2. 逐步增加领域数找“性能拐点”：固定“各领域等比例”抽样；从 k = 2 开始，每次把平均 Q 最高的新领域加入，再训练一次基础模型。记录验证集综合指标，当新增领域带来的平均提升 < x %（x待定） 时视为“拐点 k*”。
3. 锁住 k* 后调配领域比例：采用简易的三步网格或 20-30 次贝叶斯搜索：在 Simplex 上改变各领域采样比例，观察验证集指标。目标函数可以以（评价指标*x-运行时间(1-x)）最大化得到。

### 数据集选取

1. 先前MKG论文数据集
2. 开源静态 KG（ConceptNet、DBPedia、Wikidata）+ 专业领域 KG (药物/金融) + 视觉数据集 (OpenImages, LAION-400M) （药物、生物、分子、金融、推荐、else）
3. 模态补全：① 利用 CLIP 搜图 ② Web  Crawl ③ Prompt-LLM 生成文本概要

#### 构建细节

​	多模态属性图通常来自现实场景的数据，例如**社交网络平台（商品图片和文字描述）**、**生物多模态数据集（病毒蛋白质+医药、细胞+蛋白质）**等。**构建MAG数据集的首要步骤是获取****图结构**（节点及其关系）以及每个节点的**多模态属性（如文本描述、图像、蛋白质结构、病毒特征等）。构建过程中需要对原始数据进行清理和标准化：首先**清洗文本数据**，删除缺失值、非英文内容和异常符号，并截断过长文本、特征。对于图像数据，则去除没有有效图像链接的节点，确保每个保留节点至少有一张图像。随后，根据源数据（如电商平台的“also-buy/also-view”关系或社交平台的用户互动）构建节点之间的**初始关系边**，并去除自环和孤立节点以得到连通图。最后，将节点所属的类别（如商品类别或帖子所属主题）转为数值标签，作为节点分类等任务的监督信号 。

## 算力配置

* A6000*8 （科广）
* A100*8 （朋友上交组内的卡）

* autodl（北理工）

## Baseline与介绍

| Model | Type | Usage |
| :---: | :--: | :---: |
|       |      |       |
|       |      |       |
|       |      |       |
|       |      |       |
|       |      |       |

5~6



